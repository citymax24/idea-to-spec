# Feedback Round R2

| Field | Value |
|-------|-------|
| **Spec version reviewed** | 1.0 |
| **Date** | 2026-09-05 |
| **Reviewer** | citymax24 (decision delegated to Claude) |
| **Channel** | chat |
| **Confirmation** | confirmed by citymax24 on 2026-09-05 |

Types: `ADD` something is missing · `CHANGE` different from what is written · `REMOVE` out entirely · `MISREAD` the source was misunderstood · `QUESTION` why is this here · `OK` section approved as is

Statuses: `proposed` → `confirmed` | `dropped` | `deferred` → `applied (v0.x)` | `answered`

| ID | Type | Target | Verbatim (reviewer's words) | Understood as / answer | Status |
|----|------|--------|-----------------------------|------------------------|--------|
| R2-01 | CHANGE | FR-013, §10 A-05, §11 Q-10 | "Q-10  you decide" | Answer Q-10: the seven-day reminder reaches exactly one person, the user who registered the quote. Change FR-013 to name that recipient; replace assumption A-05 (which assumed the registering user *and* the office) with an assumption stating the single recipient and what it rests on; add an FR stating that the office is not notified per quote and reaches due quotes through SCR-04; delete Q-10 from §11. | applied (v1.1) |

## Notes

- The decision was delegated by the reviewer, not made by them. It is recorded here as Claude's reading of the sources, so a later reader can see it was never a human's judgement of the trade-off: `[ASSUMPTION: …]` stays the provenance tag, not `[Q-10 resolved]`.
- Evidence it rests on: F-037 (S2 row 3, sheet Rollen) makes "Erinnerung abarbeiten" the Bauleiter's daily task; F-043 (S3 07:40) asks for "einen Stups. Nicht drei.", which argues against notifying several people for one quote; F-013 and F-024 give the office its own systematic route to everything due, the Montagsliste.
- Evidence against it: F-014 (S1 §4) says "Bauleiter oder Büro ruft an", naming both as callers. That is the line A-05 was built on. It stays true for who *calls* — it just no longer decides who is *notified*.
- Applying this round moves an accepted spec: v1.0 → v1.1, status back to `in-review`, and the Approved fields are kept with the note `(v1.0)`. The spec then needs `/speckit-idea-accept` again to return to `accepted`.
- No screen changes. The reminder is a background rule (FR-013) and appears on no artboard, so the design canvas does not need rebuilding.
- Confirmed on 2026-09-05 with no corrections, and applied as v1.1.
