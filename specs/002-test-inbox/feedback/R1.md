# Feedback Round R1

| Field | Value |
|-------|-------|
| **Spec version reviewed** | 0.1 |
| **Date** | 2026-09-05 |
| **Reviewer** | citymax24 |
| **Channel** | chat |
| **Confirmation** | confirmed by citymax24 on 2026-09-05 |

Types: `ADD` something is missing · `CHANGE` different from what is written · `REMOVE` out entirely · `MISREAD` the source was misunderstood · `QUESTION` why is this here · `OK` section approved as is

Statuses: `proposed` → `confirmed` | `dropped` | `deferred` → `applied (v0.x)` | `answered`

| ID | Type | Target | Verbatim (reviewer's words) | Understood as / answer | Status |
|----|------|--------|-----------------------------|------------------------|--------|
| R1-01 | ADD | §11 Q-11, §6, §7, §5 | "Q-11 - add a screen for this" | Answer Q-11 with yes: version 1 administers the per-user accounts implied by FR-021, and it gets its own screen. Add SCR-08 · User management to §6; add FR-027 (a user of a company can be added) and FR-028 (a user of a company can be removed) to §7; add FLOW-07 so SCR-08 is reached by a flow and add SCR-08 to the leads-to of SCR-01; delete Q-11 and its `[NEEDS CLARIFICATION]` marker from §11. | applied (v0.2) |

## Notes

- R1-01 leaves one detail unnamed: which role may administer users. §2 has three roles and only the Chef is described as the one who decides ([F-007 · S1 §2]); no source covers user administration at all, which is why Q-11 existed. Unless the reviewer says otherwise at confirmation, R1-01 will be applied with SCR-08 restricted to the Chef, tagged as an assumption derived from F-007, rather than left open a second time.
- R1-01 closes the only `[NEEDS CLARIFICATION]` marker in v0.1, so the readiness checklist item "No `[NEEDS CLARIFICATION]` markers remain" flips to passing when it is applied.
- Q-10 (who receives the seven-day reminder) is untouched by this round and stays open in §11.
- Confirmed on 2026-09-05 with no corrections. The reviewer was asked which role may administer users and did not name one, so R1-01 is applied with SCR-08 restricted to the Chef, tagged as an assumption derived from F-007 (S1 §2).
